{% extends 'base.html.twig' %}
 {% block content %}

     {% block title %}

     {% endblock %}



     {% block body %}
    <h1>Reclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdRec</th>
                <td>{{ reclamation.idRec }}</td>
            </tr>
            <tr>
                <th>Objet</th>
                <td>{{ reclamation.objet }}</td>
            </tr>
            <tr>
                <th>TextR</th>
                <td>{{ reclamation.textR }}</td>
            </tr>
            <tr>
                <th>DateEnvoi</th>
                <td>{{ reclamation.dateEnvoi }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ reclamation.id }}</td>
            </tr>
            <tr>
                <th>Cours</th>
                <td>{{ reclamation.cours }}</td>
            </tr>
            <tr>
                <th>Enseignant</th>
                <td>{{ reclamation.enseignant }}</td>
            </tr>
        </tbody>
    </table>

         <button type=“button” class="button-back">  <a href="{{ path('reclamation_index') }}">back to list</a></button>

         <button type=“button” class="button-edit">  <a href="{{ path('reclamation_edit', {'idRec': reclamation.idRec}) }}">edit</a></button>
         <style>


             .button-edit {
                 display: inline-block;
                 padding: 12px 18px;
                 cursor: pointer;
                 border-radius: 5px;
                 background-color: #fde8cd;
                 font-size: 16px;
                 font-weight: bold;
                 color: red;
             }
             .button-back {
                 display: inline-block;
                 padding: 12px 18px;
                 cursor: pointer;
                 border-radius: 5px;
                 background-color: #fff3cd;
                 font-size: 16px;
                 font-weight: bold;
                 color: red;
             }
         </style>
    {{ include('reclamation/_delete_form.html.twig') }}
{% endblock %}
 {% endblock %}
