<form method="post" action="{{ path('question_delete', {'idQ': question.idQ}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ question.idQ) }}">
    <button class="btn">Delete</button>
</form>
