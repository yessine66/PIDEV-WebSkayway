<form method="post" action="{{ path('partenaire_delete', {'idP': partenaire.idP}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ partenaire.idP) }}">
    <button class="btn">Delete</button>
</form>
