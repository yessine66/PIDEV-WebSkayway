<form method="post" action="{{ path('theme_delete', {'idT': theme.idT}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ theme.idT) }}">
    <button class="btn">Delete</button>
</form>
