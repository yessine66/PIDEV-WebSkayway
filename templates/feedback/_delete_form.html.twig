<form method="post" action="{{ path('feedback_delete', {'idF': feedback.idF}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ feedback.idF) }}">
    <button class="btn">Delete</button>
</form>
