<form method="post" action="{{ path('test_delete', {'idTest': test.idTest}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ test.idTest) }}">
    <button class="btn">Delete</button>
</form>
