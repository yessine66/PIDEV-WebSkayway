{% extends 'base.html.twig' %}

{% block title %}Test index{% endblock %}

{% block body %}
    <h1>Test index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdTest</th>
                <th>DateTest</th>
                <th>Score</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for test in tests %}
            <tr>
                <td>{{ test.idTest }}</td>
                <td>{{ test.dateTest }}</td>
                <td>{{ test.score }}</td>
                <td>
                    <a href="{{ path('test_show', {'idTest': test.idTest}) }}">show</a>
                    <a href="{{ path('test_edit', {'idTest': test.idTest}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('test_new') }}">Create new</a>
{% endblock %}
