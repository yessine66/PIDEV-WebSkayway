{{ form_start(form) }}
    {{ form_widget(form) }}
    <button ype="button" class="btn btn-success swalDefaultSuccess" onsubmit="myFunction()">{{ button_label|default('Save') }}</button>


<style>
    .btn {
        display: inline-block;
        padding: 12px 18px;
        cursor: pointer;
        border-radius: 5px;
        background-color: lightgreen;
        font-size: 16px;
        font-weight: bold;
        color: white;
    }
</style>

<script>
    function myFunction() {
        alert("The form was submitted");
    }
</script>
{{ form_end(form) }}
