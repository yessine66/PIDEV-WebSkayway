<form method="post" action="{{ path('enseignant_delete', {'idEns': enseignant.idEns}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enseignant.idEns) }}">
    <button class="btn">Delete</button>
</form>
